# Required: Stripe secret key (get from https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_test_...

# Optional: Base URL used for Stripe redirect URIs.
# If omitted, the incoming request Origin header is used instead.
# Set this to your production domain when deploying.
NEXT_PUBLIC_APP_URL=https://your-domain.com
